// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model DailyPuzzle {
  id          String    @id @default(auto()) @map("_id") @db.ObjectId
  createdAt   DateTime  @default(now())
  date        String    @unique
  cards       String    @unique
  // foundSet    dailyPuzzleHistory[]
}

model User {
  username      String   @id @default(auto()) @map("_id") @db.ObjectId
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  passwordHash  String
  // dailyPuzzleHistories     dailyPuzzleHistory[]
}

// model dailyPuzzleHistory {
//   id            String   @id @default(auto()) @map("_id") @db.ObjectId
//   userId        String
//   date          String
//   createdAt     DateTime @default(now())
//   updatedAt     DateTime @updatedAt
//   user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
//   puzzle        DailyPuzzle @relation(fields: [date], references:[date], onDelete: Cascade)
// }
